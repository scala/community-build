// automatically generated by `narrow` script

build += {
  space: scala.github4s
  check-missing: [false, false]
  cross-version: [disabled, standard]
  extraction-version: ${vars.scala-version}
  sbt-version: ${vars.sbt-version}
  sbt-java-options: ${vars.sbt-java-options}
  projects: [${vars.proj.github4s}]
}
build += {
  space: scala.http4s
  check-missing: [false, false]
  cross-version: [disabled, standard]
  extraction-version: ${vars.scala-version}
  sbt-version: ${vars.sbt-version}
  sbt-java-options: ${vars.sbt-java-options}
  projects: [${vars.proj.http4s}]
}
build += {
  space: scala.refined
  check-missing: [false, false]
  cross-version: [disabled, standard]
  extraction-version: ${vars.scala-version}
  sbt-version: ${vars.sbt-version}
  sbt-java-options: ${vars.sbt-java-options}
  projects: [${vars.proj.refined}]
}
build += {
  space: scala.slick
  check-missing: [false, false]
  cross-version: [disabled, standard]
  extraction-version: ${vars.scala-version}
  sbt-version: ${vars.sbt-version}
  sbt-java-options: ${vars.sbt-java-options}
  projects: [${vars.proj.slick}]
}
build += {
  space: scala.sttp
  check-missing: [false, false]
  cross-version: [disabled, standard]
  extraction-version: ${vars.scala-version}
  sbt-version: ${vars.sbt-version}
  sbt-java-options: ${vars.sbt-java-options}
  projects: [${vars.proj.sttp}]
}
build += {
  space: scala.zinc
  check-missing: [false, false]
  cross-version: [disabled, standard]
  extraction-version: ${vars.scala-version}
  sbt-version: ${vars.sbt-version}
  sbt-java-options: ${vars.sbt-java-options}
  projects: [${vars.proj.zinc}]
}
