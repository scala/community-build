// https://github.com/monix/monix-bio.git#dcce831b607486ef7d505810ebd5dd3b67a910a3  # was master

// frozen (November 2020) at a commit that is new enough for weaver-test-monix
// but old enough for monix (which we currently have frozen at an April 2020 commit)

vars.proj.monix-bio: ${vars.base} {
  name: "monix-bio"
  uri: "https://github.com/monix/monix-bio.git#dcce831b607486ef7d505810ebd5dd3b67a910a3"

  extra.exclude: ["*JS", "benchmarks", "docs"]
}
