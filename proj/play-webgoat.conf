// https://github.com/scalacommunitybuild/play-webgoat.git#community-build-2.12  # was playframework, 2.7.x

// forked (October 2023) for JDK 21

vars.proj.play-webgoat: ${vars.base} {
  name: "play-webgoat"
  uri: "https://github.com/scalacommunitybuild/play-webgoat.git#0587d793ee15d41f26894937cdd8b25ee0413e19"

  // not sure how this gets pulled in but it's just doc not code, I think?
  // so let's just ignore that it's missing
  extra.commands: ${vars.default-commands} [
    "removeDependency com.typesafe.play play-omnidoc"
  ]
}
