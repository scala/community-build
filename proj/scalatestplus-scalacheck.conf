// https://github.com/SethTisue/scalatestplus-scalacheck.git#sbt-osgi-bump  # was scalatest, scalacheck-1.14/3.2.2.x

// temporarily forked pending merge of
// https://github.com/scalatest/scalatestplus-scalacheck/pull/38

vars.proj.scalatestplus-scalacheck: ${vars.base} {
  name: "scalatestplus-scalacheck"
  uri: "https://github.com/SethTisue/scalatestplus-scalacheck.git#4781ef222674a0e6f40f4f69a0445e567db5f717"

  extra.projects: ["scalatestPlusScalaCheckJVM"]
  extra.settings: ${vars.base.extra.settings} [
    // I don't know why, but without overriding this, dbuild chokes with e.g.:
    // scala.MatchError: d73729e1+20191231-1112-SNAPSHOT
    """version := "0.0.0""""
  ]
}
