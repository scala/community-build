// https://github.com/lagom/lagom-samples.git#1.5.x

vars.proj.lagom-samples: ${vars.base} {
  name: "lagom-samples"
  uri: "https://github.com/lagom/lagom-samples.git#e6038105f051afa4f90b8de91f49b381285e9fc2"
  // TODO add the Scala version too (may be tricky, see history at https://github.com/scala/community-builds/pull/869)
  // I figure the Java one is the most important one to have first, since there is a shortage,
  // in the community build, of repos that call into Scala from Java
  extra.directory: "shopping-cart/shopping-cart-java"
  extra.projects: ["shopping-cart"]
  // TODO add the other samples too
}
