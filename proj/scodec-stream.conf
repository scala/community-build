// https://github.com/scodec/scodec-stream.git#main

vars.proj.scodec-stream: ${vars.base} {
  name: "scodec-stream"
  uri: "https://github.com/scodec/scodec-stream.git#ff891607317cfd856ac0ef9b2b812cf1e1f0eb72"

  extra.exclude: ["*JS"]
  extra.commands: ${vars.default-commands} [
    "set every gpgWarnOnFailure := true"
  ]
}
