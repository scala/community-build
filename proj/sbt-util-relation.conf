// ivy:org.scala-sbt#util-relation_2.13;1.4.0

// This comes from the sbt/sbt repo. It would probably be feasible to
// build it from source, since it doesn't depend on the rest of sbt, but
// the sbt build is complex, so delving into it seems like a potential
// rabbit hole. Using a binary here is entirely acceptable, IMO.

vars.proj.sbt-util-relation: {
  name: "sbt-util-relation"
  system: ivy
  uri: "ivy:org.scala-sbt#util-relation_2.13;1.4.0"
}
