// https://github.com/scalacommunitybuild/upickle.git#community-build-2.13  # was lihaoyi, master

// forked to add sbt build (instead of mill)

vars.proj.upickle: ${vars.base} {
  name: "upickle"
  uri: "https://github.com/scalacommunitybuild/upickle.git#ba658457162f9b1263090a6ba843527ea4693d6c"

  extra.projects: ["core", "implicits", "upack", "ujson"]
}

// includes:
// * tests
// * integrations with argonaut, circe, json4s, play-json

vars.proj.upickle-more: ${vars.base} {
  name: "upickle-more"
  uri: "https://github.com/scalacommunitybuild/upickle.git#ba658457162f9b1263090a6ba843527ea4693d6c"

  extra.exclude: ["core", "implicits", "upack", "ujson"]  // already built above
}
