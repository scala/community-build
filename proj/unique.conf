// https://github.com/ChristopherDavenport/unique.git#eb85082eec123ff663c  # was master

// dependency of vault
// frozen (August 2019) at a commit before addition of dependency on
// discipline-specs2, which we can't add because the version of discipline
// that offers it requires a ScalaTest 3.1 upgrade

vars.proj.unique: ${vars.base} {
  name: "unique"
  uri: "https://github.com/ChristopherDavenport/unique.git#eb85082eec123ff663ce066f39b2a574d4f8b99d"
  extra.projects: ["coreJVM"]  // no docs, no Scala.js
}
