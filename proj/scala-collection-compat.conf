// https://github.com/scala/scala-collection-compat.git#master

vars.proj.scala-collection-compat: ${vars.base} {
  name: "scala-collection-compat"
  uri: "https://github.com/scala/scala-collection-compat.git#52811e6dcea1e3c936d15103b036411eee523d86"

  extra.projects: ["compat212", "binary-compat"]  // no Scala.js or Scalafix rules plz
  extra.commands: ${vars.default-commands} [
    // the following will need to be uncommented next time we run `advance`
    // // I don't understand why this became necessary when the repo added sbt-dotty
    // """set compat212JVM / Compile / unmanagedSourceDirectories += (baseDirectory in ThisBuild).value / "compat" / "src" / "main" / "scala-2.12""""
    // work around https://github.com/scala/community-builds/issues/575
    // (in a community build context, we don't need MiMa to run)
    "set every scalaModuleMimaPreviousVersion := None"
  ]
}
