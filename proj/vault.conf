// https://github.com/typelevel/vault.git#37d46d32ba909cb9b1abc540c06a4483712269fe  # was main

// dependency of http4s

// frozen (January 2021) before an org change (from io.chrisdavenport to typelevel),
// since I doubt pushing that change through everywhere will work as long as we're
// stuck on frozen fs2

vars.proj.vault: ${vars.base} {
  name: "vault"
  uri: "https://github.com/typelevel/vault.git#37d46d32ba909cb9b1abc540c06a4483712269fe"

  extra.projects: ["coreJVM"]  // no docs, no Scala.js
}
