// https://github.com/softwaremill/sttp-shared.git#v1.0.0-RC7

// dependency of sttp. we usually keep it frozen at whatever version
// sttp expects

vars.proj.sttp-shared: ${vars.base} {
  name: "sttp-shared"
  uri: "https://github.com/softwaremill/sttp-shared.git#30c6d5ab53eed695bf863f20ab555d8fcf8c9cb0"

  extra.projects: [
    // needed by sttp's core
    "core", "ws"
    // bonus integrations used downstream in sttp's integrations
    "fs2", "akka", "monix"
  ]
}
