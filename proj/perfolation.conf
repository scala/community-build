// https://github.com/outr/perfolation.git#a9fc4dfc312fda5dc8  # was master
vars.uris.perfolation-uri: "https://github.com/outr/perfolation.git#a9fc4dfc312fda5dc85f2181c55172dffd7ccbc7"

// dependency of scribe
// frozen (June 2019) at a May 2019 commit just before a 2.13 upgrade that broke
// scribe downstream. presumably eventually scribe will adapt and we can unfreeze

vars.proj.perfolation:  ${vars.base} {
  name: "perfolation"
  uri:  ${vars.uris.perfolation-uri}
  extra.projects: ["coreJVM"]
}
