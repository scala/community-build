// https://github.com/scalacommunitybuild/scala-xml-quote.git#community-build-2.12  # was allenrenucci, bintray

// dependency of scalafix
// (we use allanrenucci's fork because that's where `organization` is set
// as scalafix expects; it's where 0.1.4 was apparently published from,
// which is the version scalafix declares a dependency on)

// forked (August 2022) for scala-xml 2.x friendliness (needed to remove
// a bad test)

vars.proj.scala-xml-quote: ${vars.base} {
  name: "scala-xml-quote"
  uri: "https://github.com/scalacommunitybuild/scala-xml-quote.git#a40cd67cd506f36a4fae6b8a3c211f0d07663344"

}
