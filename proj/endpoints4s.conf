// https://github.com/endpoints4s/endpoints4s.git#master

vars.proj.endpoints4s: ${vars.base} {
  name: "endpoints4s"
  uri: "https://github.com/endpoints4s/endpoints4s.git#f06442426b1e005f7f9f3702aa52093621b24fdf"

  extra.exclude: [
    "*JS"
    "xhr-client"  // needs Scala.js
    "manual"  // out of scope
  ]
}
