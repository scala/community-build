// https://github.com/softwaremill/sttp-model.git#master

// dependency of sttp

vars.proj.sttp-model: ${vars.base} {
  name: "sttp-model"
  uri: "https://github.com/softwaremill/sttp-model.git#05889bd0937a31068bace6609db084dcafa5defc"

  // just what sttp needs
  extra.projects: ["core"]
}
