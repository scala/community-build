// https://github.com/softwaremill/sttp-model.git#28a8af23302277099ce9116cde87917b2cc0e7b1  # was master

// dependency of sttp

// frozen (April 2020) because sttp, downstream, is frozen, so this can't be advanced
// now either

vars.proj.sttp-model: ${vars.base} {
  name: "sttp-model"
  uri: "https://github.com/softwaremill/sttp-model.git#28a8af23302277099ce9116cde87917b2cc0e7b1"

  // just what sttp needs
  extra.projects: ["core"]
}
