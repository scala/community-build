// https://github.com/softwaremill/sttp-model.git#v1.2.0-RC5

// dependency of sttp. we usually keep it frozen at whatever version
// sttp expects

vars.proj.sttp-model: ${vars.base} {
  name: "sttp-model"
  uri: "https://github.com/softwaremill/sttp-model.git#ba143084a6b5375f62eeb45810408c8029d9382e"

  // just what sttp needs
  extra.projects: ["core"]
}
