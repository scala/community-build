// https://github.com/softwaremill/sttp-model.git#v1.1.4

// dependency of sttp. we usually keep it frozen at whatever version
// sttp expects

vars.proj.sttp-model: ${vars.base} {
  name: "sttp-model"
  uri: "https://github.com/softwaremill/sttp-model.git#16abad52bb8ec37511b2b2c4175cbf8823120175"

  // just what sttp needs
  extra.projects: ["core"]
}
