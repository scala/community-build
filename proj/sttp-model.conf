// https://github.com/softwaremill/sttp-model.git#v1.3.3

// dependency of sttp. we usually keep it frozen at whatever version
// sttp expects

vars.proj.sttp-model: ${vars.base} {
  name: "sttp-model"
  uri: "https://github.com/softwaremill/sttp-model.git#8910adc60bd916db3cf642e914e212b277b3e6c9"

  // just what sttp needs
  extra.projects: ["core"]
}
