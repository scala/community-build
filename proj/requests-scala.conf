// https://github.com/scalacommunitybuild/requests-scala.git#community-build-2.13  # was com-lihaoyi, master

// forked to add sbt build (instead of mill)

vars.proj.requests-scala: ${vars.base} {
  name: "requests-scala"
  uri: "https://github.com/scalacommunitybuild/requests-scala.git#07f86c085c6c972f8455c7d100848fd0f5f58120"

  // RequestTests: pending resolution of https://github.com/com-lihaoyi/requests-scala/issues/132
  // ModelTests: not investigated
  extra.commands: ${vars.default-commands} [
    """set Test / unmanagedSources / excludeFilter := HiddenFileFilter || "RequestTests.scala" || "Scala2RequestTests.scala" || "ModelTests.scala""""
  ]
}
