// https://github.com/ghik/silencer.git#master

vars.proj.silencer: ${vars.base} {
  name: "silencer"
  uri: "https://github.com/ghik/silencer.git#fc50a22a7cddc381971d8222c9ee23f44068c81a"

  // they've upgraded to ScalaTest 3.1, but we don't want to freeze,
  // because then we'd have to freeze akka as well, which now requires
  // a recent silencer. perhaps we'll ultimately have to freeze akka,
  // but for now we can get by this way
  extra.run-tests: false
}
