// https://github.com/ornicar/scalachess.git#master

vars.proj.scalachess: ${vars.base} {
  name: "scalachess"
  uri: "https://github.com/ornicar/scalachess.git#226a944e139fefd2c8d3eb7c5a26107fdf49646f"

  // these tests currently (Aug 25 2021 fail every time, even outside of dbuild;
  // reported upstream: https://github.com/ornicar/scalachess/issues/232
  extra.commands: ${vars.default-commands} [
    """set Test / unmanagedSources / excludeFilter := HiddenFileFilter || "ForsythTest.scala" || "HashTest.scala""""
  ]
}
