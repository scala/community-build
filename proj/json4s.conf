// https://github.com/json4s/json4s.git#master

vars.proj.json4s: ${vars.base} {
  name: "json4s"
  uri: "https://github.com/json4s/json4s.git#42046462c08c0ef935b794f1df6f88666a339eae"

  extra.exclude: ["*JS", "*Native"]
  extra.commands: ${vars.default-commands} [
    "set every mimaPreviousArtifacts := Set()"
    // fails in dbuild context, doesn't seem worth investigating
    """set json4sTests / Test / unmanagedSources / excludeFilter := HiddenFileFilter || "ClassFileParserSpec.scala""""
  ]
}
