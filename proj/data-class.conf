// https://github.com/alexarchambault/data-class.git#ab68b6193784ab16730dd35904dbac817c95f92a  # was main

// frozen (March 2026) at a known-green commit before the repo moved to
// a too-new version of utest

vars.proj.data-class: ${vars.base} {
  name: "data-class"
  uri: "https://github.com/alexarchambault/data-class.git#ab68b6193784ab16730dd35904dbac817c95f92a"

  // the repo's .travis.yml does the following:
  //   test test-proj-v1/publishLocal test-proj-v2/mimaReportBinaryIssues proj-v1-user/compile
  // I think trying to do something like in this in the dbuild context is probably more trouble
  // than it's actually worth
  extra.exclude: ["test-proj-*"]
}
