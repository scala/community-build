// https://github.com/oleg-py/meow-mtl.git#master

// dependency of pfps-shopping-cart

vars.proj.meow-mtl: ${vars.base} {
  name: "meow-mtl"
  uri: "https://github.com/oleg-py/meow-mtl.git#1e6f80e7700aee4bab114e0b4ce6030667c36530"

  extra.exclude: [
    "*JS"
    // this needs cats-mtl-core, which we don't currently have. we probably ought to have it,
    // but at the moment I'm just focusing on adding things that pfps-shopping-cart needs
    "monixJVM"
  ]
}
