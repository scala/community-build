// https://github.com/typelevel/discipline.git#v1.1.5

// frozen (April 2022) at a commit that's new enough for the
// discipline -> discipline-munit -> cats-effect 2 chain

vars.proj.discipline: ${vars.base} {
  name: "discipline"
  uri: "https://github.com/typelevel/discipline.git#d75e460507a9b7d69278e22583a6b150d65e5a0d"

  extra.projects: ["coreJVM"]  // no Scala.js please
  extra.commands: ${vars.default-commands} [
    "set every credentials := Seq()"
    "set every gpgWarnOnFailure := true"
  ]
}
