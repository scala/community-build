// https://github.com/monix/minitest.git#cb8c0ef5964b91e9970996bf768ec3d6cf3e5ace

// frozen at a commit not long before lawsLegacyJVM was removed, but
// the version of monix we're frozen at, downstream, needs it

vars.proj.minitest: ${vars.base} {
  name: "minitest"
  uri: "https://github.com/monix/minitest.git#cb8c0ef5964b91e9970996bf768ec3d6cf3e5ace"

  extra.projects: ["minitestJVM", "lawsJVM", "lawsLegacyJVM"]  // no Scala.js
}
