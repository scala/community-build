// https://github.com/tek/splain.git#c5376e4369ac5f9ebaa637324c757a8a9a3e8a94  # was master

// frozen (March 2021) before a dependency on zio was added (it's a
// thin dependency and it looks like we could easily remove it again
// in a fork, if we needed to?)

vars.proj.splain: ${vars.base} {
  name: "splain"
  uri: "https://github.com/tek/splain.git#c5376e4369ac5f9ebaa637324c757a8a9a3e8a94"

  extra.commands: [
    "set every gpgWarnOnFailure := true"
  ]
}
